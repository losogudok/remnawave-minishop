# Dev-only tooling. Install runtime dependencies separately with:
# python -m pip install -r backend/requirements.txt -r requirements-dev.txt
mypy>=2.3.0,<3
pre-commit>=4.6.0,<5
pytest>=9.1.1,<10
pytest-cov>=7.1.0,<8
ruff==0.16.0
